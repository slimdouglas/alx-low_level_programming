#include"main.h"

/**
 * _abs - returns absolute value of a ch
 * @n: int n
(*
 * Description: abs value of input)?
(* section header: Section description)*
 * Return: returns absolute value of n
 */

int _abs(int n)
{
	if (n < 0)
	{
		n = (-1) * n;
		return (n);
	}
}

#include<stdio.h>


/**
 * main - Short description, single line
 * @void : Description of parameter x
(* a blank line
 * Description: Prints all base 16 digits)?
(* section header: Section description)*
 * Return: Returns zero
 */
int main(void)
{
	unsigned char m;

	for (m = 0; m <= 'F'; m++)
	{
		putchar(m);
	}
	putchar('\n');
	return (0);
}

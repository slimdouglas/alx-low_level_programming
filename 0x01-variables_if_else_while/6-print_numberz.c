#include<stdio.h>

/**
 * main - Short description, single line
 * @void: Means no output x
(* a blank line
 * Description: Longer description of the function)?
(* section header: Section description)*
 * Return: Description of the returned value
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		putchar((a) + '0');
	}
	putchar('\n');
	return (0);
}
